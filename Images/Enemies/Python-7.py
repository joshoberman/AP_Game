def measure_distance(curr_pos, target_range):
    time1 = core.getTime()
    distance = abs(curr_pos-target_pos)
    self.player_pos.append(curr_pos)
    if self.enemy.middle==target_pos:
        time2 = core.getTime()
        return time2
    reactionTime = time2-time1
        
def get_range(pos):
    return range(pos, pos+32, 1)
a_pos = 10        
a_range = get_range(a_pos)
b_pos = SCREEN_WIDTH//3
b_range = get_range(b_pos)
c_pos = SCREEN_WIDTH - 42
c_range = get_range(c_pos)
d_pos = 2*(SCREEN_WIDTH//3)
d_range = get_range(d_pos)
    
        self.in_range_A = False
        self.in_range_B = False
        self.in_range_C = False
        self.in_range_D = False
        def grab_time(enemy_range):
            if enemy_range = True:
                time = Core.getTime()
                if enemy_range == self.in_range_A:
                    self.enemyA_range.append(time)
                    self.in_range_A = False
                if enemy_range == self.in_range_B:
                    self.enemyB_range.append(time)
                    self.in_range_B = False
                if enemy_range == self.in_range_C:
                    self.enemyA_range.append(time)
                    self.in_range_C = False

    def get_range(pos):
        return range(pos, pos+33, 1)
    a_pos = 10        
    a_range = get_range(a_pos)
    b_pos = SCREEN_WIDTH//3
    b_range = get_range(b_pos)
    c_pos = SCREEN_WIDTH - 42
    c_range = get_range(c_pos)
    d_pos = 2*(SCREEN_WIDTH//3)
    d_range = get_range(d_pos)
                                
        
